/**
 * React components for dash4devs-platform.
 *
 *   import { DashImage } from "dash4devs-platform/react";
 *
 * DashImage is a drop-in for <img>/<Image>: pass a bare `src`, or an `image`
 * object carrying pre-generated responsive variants + LQIP for blur-up loading
 * from the CDN.
 */

export { DashImage, default as DashImageDefault } from "./DashImage.jsx";
